import styled from "@emotion/styled";

const Error = styled.div`
  margin: 20px;

  .link {
    text-decoration: none;
  }
`;

const Back = styled.div`
  display: flex;
  align-items: center;
  color: rgb(0 0 0);
  font-size: 18px;
  font-weight: 700;

  .icon {
    margin-right: 10px;
  }
`;

export { Error, Back };
