import React from "react";

const LoginLogic = () => {
  const login = (e) => {
    e.preventDefault();
  };

  const register = (e) => {
    e.preventDefault();
  };

  return { login, register };
};

export default LoginLogic;
